{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <ul class="lang-menu">
            <li><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'zh'})) }}">中文</a></li>
            <li><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'en'})) }}">English</a></li>
        </ul>
        <div id="container">
            <a href="{{ path('hwi_oauth_service_redirect', {'service': 'facebook' }) }}" alt="Sign in with Facebook">Sign in with Facebook</a>
            <a href="{{ path('hwi_oauth_service_redirect', {'service': 'google' }) }}" alt="Sign in with Google">Sign in with Google</a>
            <a href="{{ path('hwi_oauth_service_redirect', {'service': 'linkedin' }) }}" alt="Sign in with LinkedIn">Sign in with LinkedIn</a>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}

{% endblock %}

{% block javascripts %}

{% endblock %}
